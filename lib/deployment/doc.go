/*
Package deployment is a set of interfaces and wrappers around Docker, Fleet and
other scheduling backends to allow for a generic method of submitting jobs.

This package aims to not reinvent the wheel without good reason.
*/
package deployment
